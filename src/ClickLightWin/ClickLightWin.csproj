<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <LangVersion>latest</LangVersion>
    <RootNamespace>ClickLightWin</RootNamespace>
    <AssemblyName>ClickLightWin</AssemblyName>
    <Product>ClickLight</Product>
    <Description>Highlights your mouse clicks on screen during demos and screen sharing.</Description>
    <!-- Version is derived from the latest git tag by MinVer (see below). -->
    <MinVerTagPrefix>v</MinVerTagPrefix>
    <!-- The LibraryImport source generator emits unsafe marshalling code. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>Assets\icon.ico</ApplicationIcon>
    <Platforms>x64;arm64</Platforms>
    <!--
      WFO0003: WinForms wants high-DPI configured via its API. This is a WPF app that
      uses WinForms only for NotifyIcon/Screen (never its app host), and PerMonitorV2 is
      declared in app.manifest by design. The WinForms path does not apply here.
    -->
    <NoWarn>WFO0003</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ClickLightWin.Tests" />
  </ItemGroup>

  <ItemGroup>
    <!-- App icon (tray + window) and the wordmark shown on the General settings tab. -->
    <Resource Include="Assets\icon.ico" />
    <Resource Include="Assets\clicklight-logo.png" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="MinVer" Version="7.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Velopack" Version="1.2.0" />
  </ItemGroup>

</Project>
