<Window x:Class="ClickLightWin.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        IsHitTestVisible="False"
        WindowStartupLocation="Manual">
  <Grid>
    <Canvas x:Name="PulseCanvas" IsHitTestVisible="False" Background="Transparent" />
    <!-- Live shortcut display: pills stack upward from the bottom-center. -->
    <StackPanel x:Name="ShortcutStack" IsHitTestVisible="False"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Margin="0,0,0,64" />
    <!-- Draw-mode indicator: a border + badge shown while click-through is off. -->
    <Border x:Name="DrawModeBorder" IsHitTestVisible="False" Visibility="Collapsed"
            BorderBrush="#FF2905" BorderThickness="3" Background="Transparent">
      <Border VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0"
              Background="#E6202124" CornerRadius="8" Padding="14,7">
        <TextBlock Text="Drawing — Ctrl+Shift+D to exit" Foreground="White"
                   FontFamily="Segoe UI Variable Text, Segoe UI" FontSize="13" FontWeight="SemiBold" />
      </Border>
    </Border>
  </Grid>
</Window>
